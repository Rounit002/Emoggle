# PostgreSQL credentials (discrete)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Emoggle
DB_USER=postgres
DB_PASSWORD="Password12@#"
DATABASE_URL=postgresql://postgres:Password12%40%23@localhost:5432/Emoggle
PORT=3001

# JWT authentication
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Google OAuth (from Google Cloud Console → Credentials → OAuth 2.0 Client ID)
GOOGLE_CLIENT_ID=

# Frontend origin for CORS (e.g. http://localhost:3000 or https://yourdomain.com)
FRONTEND_URL=http://localhost:3000
